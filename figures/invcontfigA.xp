/* slightly modified sample from epix samples gallery */
#include "epix.h"
#include <math.h>
using namespace ePiX;

int main()
{
  double slope;
  double x,y;
  double extra1,extra2;

  picture(P(-2,-2), P(2,2), "2.3x2.3in");

  begin();
  //border();

  h_axis(P(-2,0),P(2,0),2);
  v_axis(P(0,-2),P(0,2),2);

  bold();
  line(P(-2,-2),P(0,0));
  line(P(0,1),P(1,2));

  dot_size(3);
  dot(P(0,0));
  circ(P(0,1));


  tikz_format();
  end();
}

